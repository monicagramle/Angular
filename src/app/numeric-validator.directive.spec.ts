import { NumericValidatorDirective } from './numeric-validator.directive';

describe('NumericValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
